// function findMatching(drivers, driverName) {
//     drivers.filter(function() {return driverName === 'Bobby'})
// }
// const findMatching = drivers.filter(driverName => driverName==='Bobby')

function findMatching(drivers, name) {
    return drivers.filter(name => name ==='Bobby')
}